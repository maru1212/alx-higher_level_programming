#!/usr/bin/python3
def delet_at(my_list=[], idx=0):
    if idx >= 0 and idx < len(my_list):
        new_list = ""
        for i in my_list:
            if i != my_list[idx]:
                new_list += i
                return(new_list)
    else:
        return(my_list)
