#!/usr/bin/python3
def print_reversed_list_integer(my_list=[]):
#   i = len(my_list)
    for i in my_list:
        i = len(my_list) - i - 1
        print(my_list[i])
        if i < 0:
            break
