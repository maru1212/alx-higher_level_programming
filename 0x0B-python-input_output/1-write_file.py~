#!/usr/bin/python3
""" Author: Maranata Feleke. """


def write_file(filename="", text=""):
    """ A function which enables users to write in to a file.
    Args:
        filename(str): The name of the file.
        text(str): The text to writen in the file.
    Returns:
         The characters writen"""

    with open(filename, mode="w", encoding="UTF-8") as f:
        f.write(text)
